import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  titleText: {
    fontSize: 22,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingVertical: 20,
    color: 'darkblue',
  },
});

export default style;
